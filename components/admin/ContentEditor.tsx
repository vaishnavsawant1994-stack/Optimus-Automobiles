'use client'

import { LoaderCircle, Save } from 'lucide-react'
import { useRouter } from 'next/navigation'
import { useState, type FormEvent } from 'react'

type Revision = { id: string; version: number; status: string; createdAt: string; author: { name: string | null; email: string } }
export function ContentEditor({ pageKey, value, version, status, revisions }: { pageKey: string; value: unknown; version: number; status: string; revisions: Revision[] }) { const router = useRouter(); const [message, setMessage] = useState(''); const [busy, setBusy] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); let parsed: unknown; try { parsed = JSON.parse(String(form.get('value'))) } catch { setMessage('Content must be valid JSON.'); return } setBusy(true); const response = await fetch(`/api/admin/content/${pageKey}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ value: parsed, status: form.get('status'), version }) }); const body = await response.json(); setBusy(false); setMessage(body.message ?? body.error?.message ?? 'Save failed.'); if (response.ok) router.refresh() } return <div className="admin-detail-layout"><section className="admin-panel admin-panel--full"><header><h2>Structured page content</h2></header><div className="admin-panel__body"><form className="admin-form" onSubmit={submit}><label>Content JSON<textarea name="value" defaultValue={JSON.stringify(value, null, 2)} style={{ minHeight: 420, fontFamily: 'monospace' }} spellCheck={false} /></label><label>Publication status<select name="status" defaultValue={status}><option>DRAFT</option><option>IN_REVIEW</option><option>PUBLISHED</option><option>ARCHIVED</option></select></label>{message ? <p className="admin-form-message" role="status">{message}</p> : null}<button className="admin-button" disabled={busy}>{busy ? <LoaderCircle /> : <Save />}Save content</button></form></div></section><aside className="admin-panel"><header><h2>Revision history</h2></header><div className="admin-panel__body admin-timeline">{revisions.map((item) => <article key={item.id}><strong>Version {item.version} - {item.status}</strong><p>{item.author.name ?? item.author.email}</p><time>{new Date(item.createdAt).toLocaleString('en-IN')}</time></article>)}</div></aside></div> }
